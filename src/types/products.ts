export type Product = {
    discountPercent: number;
    isNew: boolean; // Assuming it indicates if the product is new
    quantity: number;
    _id: string;
    name: string;
    description: string;
    price: number;
    imageUrl: string;
    rating: number;
    size: string;
    colors: string;
  };
  